{- work in progress -}

module App.WaveForm
where

import STM32.MachineInterface
import STM32.Utils
import STM32.GPIO as GPIO

-- type Signal = array Word8 (Maybe Bool)
-- type WaveForm = [(Pin,Signal)]

